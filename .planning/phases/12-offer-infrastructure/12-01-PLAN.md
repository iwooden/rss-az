---
phase: 12-offer-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/state.pyx
  - core/state.pxd
  - entities/player.pyx
  - entities/player.pxd
  - VECTORS.md
autonomous: true

must_haves:
  truths:
    - "Player acquisition_proceeds field exists and can store cash values"
    - "Hidden state has offer buffer with offer_count and offer_index tracking"
    - "State layout computes correct offsets for new hidden fields"
  artifacts:
    - path: "core/state.pyx"
      provides: "Hidden offer buffer layout, player acquisition_proceeds in layout"
      contains: "hidden_offer_buffer_offset"
    - path: "entities/player.pyx"
      provides: "Player acquisition_proceeds getter/setter"
      contains: "acquisition_proceeds"
  key_links:
    - from: "entities/player.pyx"
      to: "core/state.pyx"
      via: "StateLayout player_fields"
      pattern: "acquisition_proceeds"
---

<objective>
Add state infrastructure for acquisition offers: hidden offer buffer in state layout and player acquisition_proceeds field.

Purpose: The acquisition phase needs hidden state to store the pre-computed offer buffer (corp_id, company_id tuples) and tracking indices. Players need an acquisition_proceeds field to track cash received from selling private companies during the phase (prevents re-acquisition loops).

Output: Updated state.pyx with hidden offer buffer layout, updated player.pyx with acquisition_proceeds field, updated VECTORS.md documentation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-offer-infrastructure/12-CONTEXT.md
@.planning/phases/12-offer-infrastructure/12-RESEARCH.md
@core/state.pyx
@entities/player.pyx
@VECTORS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hidden offer buffer to state layout</name>
  <files>core/state.pyx, core/state.pxd</files>
  <action>
Extend the StateLayout struct and compute_layout() function to add hidden state for the offer buffer:

1. In state.pxd, add to StateLayout struct:
   - hidden_offer_buffer_offset (int)
   - hidden_offer_count_offset (int)
   - hidden_offer_index_offset (int)

2. In state.pyx compute_layout(), after existing hidden state (after hidden_corp_price_indices):
   - Add offer_count slot (1 float) - tracks number of offers in buffer
   - Add offer_index slot (1 float) - tracks current offer being processed
   - Add offer_buffer (500 floats = 250 offers * 2 floats per offer)
   - Each offer is (corp_id, company_id) as 2 consecutive floats
   - Update hidden_size calculation

3. Add DEF constant at top: OFFER_BUFFER_SIZE = 250

4. Update total hidden size comment to reflect new fields.

Follow existing hidden state pattern (see hidden_deck_order as reference - lines 183-196).
  </action>
  <verify>
python setup.py build_ext --inplace && python -c "from core.state import GameState; gs = GameState(3); gs.initialize_game(); print('Layout OK, hidden size:', gs._layout.hidden_size)"
  </verify>
  <done>State layout includes hidden_offer_buffer_offset, hidden_offer_count_offset, hidden_offer_index_offset. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Add player acquisition_proceeds field</name>
  <files>core/state.pyx, core/state.pxd, entities/player.pyx, entities/player.pxd</files>
  <action>
Add acquisition_proceeds field to player state following existing pattern:

1. In state.pxd PlayerFieldOffsets struct, add:
   - acquisition_proceeds (int)

2. In state.pyx compute_player_field_offsets(), after share_sells:
   - Add acquisition_proceeds offset
   - Increment offset by 1

3. Update player_stride calculation in compute_layout() to include +1 for acquisition_proceeds.

4. In player.pxd, add to Player class:
   - cdef int _acquisition_proceeds_offset

5. In player.pyx Player class:
   - In initialize(), cache _acquisition_proceeds_offset
   - Add get_acquisition_proceeds(GameState state) -> int (uses CASH_DIVISOR)
   - Add set_acquisition_proceeds(GameState state, int proceeds)
   - Add add_acquisition_proceeds(GameState state, int amount)
   - Add clear_acquisition_proceeds(GameState state) - sets to 0

Follow existing pattern from get_cash/set_cash/add_cash.
  </action>
  <verify>
python setup.py build_ext --inplace && python -c "
from core.state import GameState
from entities.player import PLAYERS
gs = GameState(3)
gs.initialize_game()
PLAYERS[0].set_acquisition_proceeds(gs, 25)
assert PLAYERS[0].get_acquisition_proceeds(gs) == 25
PLAYERS[0].add_acquisition_proceeds(gs, 10)
assert PLAYERS[0].get_acquisition_proceeds(gs) == 35
PLAYERS[0].clear_acquisition_proceeds(gs)
assert PLAYERS[0].get_acquisition_proceeds(gs) == 0
print('Player acquisition_proceeds OK')
"
  </verify>
  <done>Player has acquisition_proceeds field with get/set/add/clear methods. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Update VECTORS.md documentation</name>
  <files>VECTORS.md</files>
  <action>
Update VECTORS.md to document the new state fields:

1. In the Player section, add acquisition_proceeds field to the table:
   - Field: acquisition_proceeds
   - Size: 1
   - Encoding: normalized (/ CASH_DIVISOR)
   - Notes: Cash from selling private companies this phase

2. Update player stride formula to include +1 for acquisition_proceeds.

3. Update "Player Field Offsets" table to include acquisition_proceeds after share_sells.

4. In Hidden State Layout section, add new fields:
   - offer_count (1 float) - Number of offers in buffer
   - offer_index (1 float) - Current offer being processed
   - offer_buffer (500 floats) - Offer tuples (corp_id, company_id)

5. Update Hidden Size table (currently 52) to reflect new size (52 + 1 + 1 + 500 = 554).

Note: The visible state sizes will increase by 1 per player due to acquisition_proceeds. Update the Size Calculation table accordingly:
- 2 players: 2941 + 2 = 2943 visible
- 3 players: 3020 + 3 = 3023 visible
- etc.
  </action>
  <verify>
grep -q "acquisition_proceeds" VECTORS.md && grep -q "offer_buffer" VECTORS.md && echo "VECTORS.md updated"
  </verify>
  <done>VECTORS.md documents player acquisition_proceeds field and hidden offer buffer fields.</done>
</task>

</tasks>

<verification>
1. Build succeeds: python setup.py build_ext --inplace
2. Existing tests pass: pytest tests/ -v
3. New state fields accessible:
   - GameState._layout has hidden_offer_buffer_offset
   - Player has get/set/add/clear_acquisition_proceeds methods
4. VECTORS.md accurately reflects new layout
</verification>

<success_criteria>
- Hidden state layout includes offer buffer (250 slots, 500 floats)
- Player entity has acquisition_proceeds field with full accessor methods
- State sizes updated correctly (hidden size increases, visible size increases per player)
- All existing tests continue to pass
- VECTORS.md documentation matches implementation
</success_criteria>

<output>
After completion, create `.planning/phases/12-offer-infrastructure/12-01-SUMMARY.md`
</output>
