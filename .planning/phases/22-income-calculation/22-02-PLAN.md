---
phase: 22-income-calculation
plan: 02
type: tdd
wave: 2
depends_on: ["22-01"]
files_modified:
  - entities/corp.pyx
  - tests/phases/test_income.py
autonomous: true

must_haves:
  truths:
    - "Corporation with PR ability receives +1 per company owned"
    - "Corporation with DA ability adds printed income of highest FV company again (doubling effect)"
    - "Corporation with S ability receives synergy_markers // 2 bonus"
    - "Corporation with VM ability reduces total CoO by min(total_coo, 10)"
    - "Corporations without special abilities are unaffected"
  artifacts:
    - path: "entities/corp.pyx"
      provides: "Enhanced calculate_income with special abilities"
      contains: "CORP_PR|CORP_DA|CORP_S|CORP_VM"
    - path: "tests/phases/test_income.py"
      provides: "Special ability tests"
      contains: "TestCorpSpecialAbilities"
  key_links:
    - from: "entities/corp.pyx"
      to: "core/data.pxd"
      via: "CorpIndices enum import"
      pattern: "from core.data cimport.*CorpIndices"
---

<objective>
Enhance Corporation.calculate_income() with special ability bonuses for PR, DA, S, and VM corporations.

Purpose: Complete the corporation income formula by adding special ability modifiers per RULES.md line 415-422. Each of the 8 corporations has a unique ability; 4 of them affect income calculation.

Output: Corporation.calculate_income() correctly applies ability bonuses with comprehensive tests for each.
</objective>

<execution_context>
@/home/icebreaker/.claude/get-shit-done/workflows/execute-plan.md
@/home/icebreaker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-income-calculation/22-CONTEXT.md
@.planning/phases/22-income-calculation/22-RESEARCH.md
@.planning/phases/22-income-calculation/22-01-SUMMARY.md

# Key source files
@entities/corp.pyx
@core/data.pxd (CorpIndices enum at lines 38-47)
@tests/phases/test_income.py
</context>

<feature>
  <name>Corporation Special Abilities (CSA-01, CSA-02, CSA-03, CSA-04)</name>
  <files>entities/corp.pyx, tests/phases/test_income.py</files>
  <behavior>
    Special Ability Modifications to calculate_income:

    **CSA-01 - Prussian Railway (PR, CORP_PR=4):**
    - Formula addition: +company_count
    - Example: Corp owns 3 companies -> +3 bonus
    - Test: PR with 0/1/3 companies

    **CSA-02 - Doppler AG (DA, CORP_DA=5):**
    - Formula addition: +highest_fv_income (doubles printed income of highest FV company)
    - IMPORTANT: Use printed income, NOT adjusted income
    - Example: Corp owns companies with FV 1,3,5 and incomes 2,4,6 -> +6 (highest FV=5 has income 6)
    - Test: DA with multiple companies, verify highest FV selected

    **CSA-03 - Synergistic (S, CORP_S=1):**
    - Formula addition: +synergy_markers // 2
    - Uses marker_count from compute_synergy_bonuses, NOT synergy_income
    - Example: 5 synergy markers -> +2 bonus
    - Test: S with 0/2/5 synergy markers

    **CSA-04 - Vintage Machinery (VM, CORP_VM=6):**
    - Formula modification: total_coo = max(0, total_coo - 10)
    - Apply BEFORE subtracting CoO from income
    - Example: total_coo=15 -> becomes 5; total_coo=8 -> becomes 0
    - Test: VM with CoO < 10, CoO = 10, CoO > 10

    Other corporations (JS=0, OS=2, SM=3, SI=7) have abilities that don't affect income calculation - no changes needed.

    Test cases to write first:
    1. PR with 0 companies -> 0 bonus (but calculate_income still works)
    2. PR with 3 companies -> +3 bonus
    3. DA with 2 companies (FV 1, 3) -> bonus = income of FV=3 company
    4. S with 4 markers -> +2 bonus
    5. S with 5 markers -> +2 bonus (rounds down)
    6. VM with total_coo=8 -> CoO reduced to 0
    7. VM with total_coo=15 -> CoO reduced to 5
    8. Non-special corp (JS) -> no ability bonus
  </behavior>
  <implementation>
    1. Ensure CorpIndices is cimported in corp.pyx (from core.data cimport CorpIndices)
    2. Modify calculate_income to track additional values during company iteration:
       - company_count (already needed for PR)
       - highest_fv and highest_fv_income (for DA)
       - synergy_markers is already returned by compute_synergy_bonuses
    3. After base calculation, add switch logic:
       - if self.corp_id == CorpIndices.CORP_VM: apply CoO reduction FIRST
       - if self.corp_id == CorpIndices.CORP_PR: add company_count
       - if self.corp_id == CorpIndices.CORP_DA: add highest_fv_income
       - if self.corp_id == CorpIndices.CORP_S: add synergy_markers // 2
    4. VM must be applied before final calculation since it modifies CoO

    Key insight from CONTEXT.md:
    - DA doubles PRINTED income (before CoO), not adjusted income
    - VM reduces TOTAL CoO, not per-company CoO
    - S uses marker_count // 2, not synergy_income
  </implementation>
</feature>

<verification>
1. `python setup.py build_ext --inplace` compiles without errors
2. `pytest tests/phases/test_income.py::TestCorpSpecialAbilities -v` - all ability tests pass
3. `pytest tests/ -v` - full test suite passes
4. grep confirms ability switch logic exists: `grep -E "CORP_PR|CORP_DA|CORP_S|CORP_VM" entities/corp.pyx`
</verification>

<success_criteria>
- PR corporation receives +1 per company owned
- DA corporation receives printed income of highest FV company as bonus
- S corporation receives synergy_markers // 2 as bonus
- VM corporation has total CoO reduced by up to 10
- Non-income-affecting abilities (JS, OS, SM, SI) don't modify calculation
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/22-income-calculation/22-02-SUMMARY.md`
</output>
