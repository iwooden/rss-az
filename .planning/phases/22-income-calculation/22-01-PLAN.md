---
phase: 22-income-calculation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - entities/corp.pyx
  - entities/fi.pyx
  - core/data.pxd
  - tests/phases/test_income.py
autonomous: true

must_haves:
  truths:
    - "Corporation.calculate_income returns sum of printed income minus CoO for owned companies"
    - "Corporation.calculate_income adds synergy income from compute_synergy_bonuses"
    - "ForeignInvestor.calculate_income returns sum of printed income minus CoO plus 5"
    - "Empty corporation (no companies) returns 0 income"
    - "FI with no companies returns 5 (base bonus only)"
  artifacts:
    - path: "entities/corp.pyx"
      provides: "Corporation.calculate_income method"
      contains: "cpdef int calculate_income"
    - path: "entities/fi.pyx"
      provides: "ForeignInvestor.calculate_income method"
      contains: "cpdef int calculate_income"
    - path: "tests/phases/test_income.py"
      provides: "Income calculation tests"
      contains: "TestCorpBaseIncome"
  key_links:
    - from: "entities/corp.pyx"
      to: "core/data.pyx"
      via: "compute_synergy_bonuses call"
      pattern: "compute_synergy_bonuses\\("
    - from: "entities/corp.pyx"
      to: "core/data.pyx"
      via: "get_company_income, get_company_stars, get_cost_of_ownership"
      pattern: "get_company_income|get_company_stars|get_cost_of_ownership"
---

<objective>
Implement base income calculation for Corporation and ForeignInvestor entities using TDD.

Purpose: Establish the foundation for income calculation that Phase 23's INCOME phase handler will use. Corporation income includes synergy bonus from Phase 21. FI income includes fixed +5 bonus per game rules.

Output: Working calculate_income() methods on Corporation and ForeignInvestor classes with comprehensive tests.
</objective>

<execution_context>
@/home/icebreaker/.claude/get-shit-done/workflows/execute-plan.md
@/home/icebreaker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-income-calculation/22-CONTEXT.md
@.planning/phases/22-income-calculation/22-RESEARCH.md
@.planning/phases/21-synergy-infrastructure/21-01-SUMMARY.md

# Key source files
@entities/corp.pyx
@entities/fi.pyx
@entities/player.pyx (lines 444-463 for get_income pattern)
@core/data.pyx (compute_synergy_bonuses at lines 270-310)
@core/data.pxd (accessors and CorpIndices enum)
@tests/phases/test_income.py
</context>

<feature>
  <name>Base Income Calculation (INC-01, INC-02, INC-03, SYN-03)</name>
  <files>entities/corp.pyx, entities/fi.pyx, core/data.pxd, tests/phases/test_income.py</files>
  <behavior>
    Corporation.calculate_income(state) -> int:
      - Iterate owned companies, sum printed income and CoO
      - Compute synergy bonus via compute_synergy_bonuses()
      - Return: gross_printed_income - total_coo + synergy_income
      - Input: GameState with corporation owning companies
      - Output: Integer income (can be negative)
      - Edge cases: 0 companies -> 0, 1 company -> base-coo (no synergy)

    ForeignInvestor.calculate_income(state) -> int:
      - Iterate owned companies, sum printed income and CoO
      - Add fixed +5 bonus per RULES.md line 354
      - Return: gross_printed_income - total_coo + 5
      - Input: GameState with FI owning companies
      - Output: Integer income (always positive due to CLOSING phase)
      - Edge cases: 0 companies -> 5 (just the bonus)

    Test cases to write first:
    1. Corp with 0 companies -> 0
    2. Corp with 1 company -> income - coo (no synergy)
    3. Corp with 2+ synergizing companies -> income - coo + synergy
    4. FI with 0 companies -> 5
    5. FI with 1 company -> income - coo + 5
    6. Corp at different CoO levels -> correct deduction
  </behavior>
  <implementation>
    1. Add cimport for compute_synergy_bonuses to entities/corp.pyx
    2. In Corporation class, add calculate_income method following Player.get_income pattern
    3. In ForeignInvestor class, add calculate_income method with +5 bonus
    4. Use turn_module.TURN.get_coo_level(state) for CoO lookup (same as Player)
    5. Use get_company_income, get_company_stars, get_cost_of_ownership accessors
    6. For Corp, collect owned company IDs into array, call compute_synergy_bonuses

    IMPORTANT: Do NOT implement special abilities here - that's Plan 22-02.
    Only implement: base income - CoO + synergy (for Corp) or base income - CoO + 5 (for FI).
  </implementation>
</feature>

<verification>
1. `python setup.py build_ext --inplace` compiles without errors
2. `pytest tests/phases/test_income.py -v` - all new income tests pass
3. `pytest tests/ -v` - full test suite passes (320+ tests)
4. grep confirms calculate_income methods exist in both corp.pyx and fi.pyx
</verification>

<success_criteria>
- Corporation.calculate_income returns correct value for various company configurations
- ForeignInvestor.calculate_income returns correct value with +5 bonus
- Synergy income is correctly added to corporation income
- CoO is correctly deducted based on current CoO level and company star tiers
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/22-income-calculation/22-01-SUMMARY.md`
</output>
