---
phase: 23-phase-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - entities/corp.pyx
  - phases/invest.pyx
  - core/state.pyx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Corporation bankruptcy executes from a single method in entities/corp.pyx"
    - "invest.pyx calls Corp.go_bankrupt() instead of inline _execute_bankruptcy()"
    - "No duplicate bankruptcy code exists in codebase"
    - "Incomplete bankrupt_corp() stub is removed from core/state.pyx"
  artifacts:
    - path: "entities/corp.pyx"
      provides: "go_bankrupt() method on Corporation class"
      contains: "cpdef void go_bankrupt"
    - path: "phases/invest.pyx"
      provides: "Updated sell handler calling Corp.go_bankrupt()"
      contains: "corp.go_bankrupt(state)"
  key_links:
    - from: "phases/invest.pyx"
      to: "entities/corp.pyx"
      via: "Corp.go_bankrupt() call"
      pattern: "corp\\.go_bankrupt\\(state\\)"
---

<objective>
Refactor bankruptcy code to single location in Corp entity for reuse by INCOME phase.

Purpose: INCOME phase needs to call bankruptcy when corporation cannot pay negative income.
Currently `_execute_bankruptcy()` is inline in invest.pyx (57 lines). Moving to Corp.go_bankrupt()
enables both INVEST and INCOME phases to use the same code.

Output: Corp.go_bankrupt() method, updated invest.pyx, deleted stub in state.pyx
</objective>

<execution_context>
@/home/icebreaker/.claude/get-shit-done/workflows/execute-plan.md
@/home/icebreaker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-phase-integration/23-CONTEXT.md
@.planning/phases/23-phase-integration/23-RESEARCH.md

# Existing bankruptcy code
@phases/invest.pyx (lines 110-167: _execute_bankruptcy function)
@core/state.pyx (lines 606-610: bankrupt_corp stub to DELETE)
@entities/corp.pyx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Corp.go_bankrupt() method</name>
  <files>entities/corp.pyx</files>
  <action>
Add `cpdef void go_bankrupt(self, GameState state)` method to Corporation class after the apply_income() method (around line 400).

Move the logic from `phases/invest.pyx:_execute_bankruptcy()` (lines 110-167) into this method. The logic is:

1. Remove all owned companies from game (clear ownership, call company.remove_from_game)
2. Return all shares to unissued (clear player shares, clear presidencies)
3. Reset corp share counts (unissued = total, issued = 0, bank = 0)
4. Return money to bank (set cash to 0)
5. Free market space if needed
6. Deactivate corp and clear remaining state (active=False, price_index=0, receivership=False, income=0, stars=0, acquisition_proceeds=0)
7. Clear acquisition company flags

Use self.corp_id instead of corp_id parameter. Import get_corp_share_count from core.data if not already imported.

Required imports to add at top of corp.pyx:
- from entities import company as company_module
- from entities import market as market_module

The method signature must be `cpdef` (not `cdef`) so it can be called from other modules.
  </action>
  <verify>Build succeeds: `python setup.py build_ext --inplace`</verify>
  <done>Corporation class has go_bankrupt() method with full bankruptcy logic</done>
</task>

<task type="auto">
  <name>Task 2: Update invest.pyx to call Corp.go_bankrupt()</name>
  <files>phases/invest.pyx</files>
  <action>
In `phases/invest.pyx`:

1. DELETE the `_execute_bankruptcy()` function (lines 110-167 approximately)

2. In `_handle_sell_share()` function, find the bankruptcy check (around line 292-300):
   ```
   if new_index == 0:
       _execute_bankruptcy(state, corp_id)
       ...
   ```

   Replace `_execute_bankruptcy(state, corp_id)` with:
   ```
   corp.go_bankrupt(state)
   ```

   Note: `corp` is already defined earlier in the function as `corp = corp_module.CORPS[corp_id]`
  </action>
  <verify>Build succeeds: `python setup.py build_ext --inplace` and `pytest tests/test_invest.py -v` passes</verify>
  <done>invest.pyx calls Corp.go_bankrupt() instead of inline function</done>
</task>

<task type="auto">
  <name>Task 3: Delete bankrupt_corp() stub from state.pyx</name>
  <files>core/state.pyx</files>
  <action>
Delete the incomplete `bankrupt_corp()` stub from `core/state.pyx` (lines 606-610):

```python
cpdef void bankrupt_corp(self, int corp_id):
    """Mark corporation as bankrupt (inactive, price index 0)."""
    self.set_corp_active(corp_id, False)
    self.set_corp_price_index(corp_id, 0)
    self.set_corp_in_receivership(corp_id, False)
```

This stub is incomplete (doesn't handle shares, companies, cash, market space) and is no longer needed.
  </action>
  <verify>Build succeeds: `python setup.py build_ext --inplace` and grep confirms no bankrupt_corp in state.pyx: `grep -n "bankrupt_corp" core/state.pyx` returns nothing</verify>
  <done>Incomplete bankrupt_corp stub removed from state.pyx</done>
</task>

</tasks>

<verification>
1. Build: `python setup.py build_ext --inplace` succeeds
2. All existing tests pass: `pytest tests/ -v`
3. Bankruptcy behavior unchanged: existing bankruptcy tests in test_invest.py still pass
4. No duplicate code: `grep -r "_execute_bankruptcy" phases/` returns nothing
5. Stub removed: `grep -n "bankrupt_corp" core/state.pyx` returns nothing
</verification>

<success_criteria>
- Corp.go_bankrupt() method exists and contains full bankruptcy logic
- phases/invest.pyx calls corp.go_bankrupt(state) on price index 0
- _execute_bankruptcy() function removed from invest.pyx
- bankrupt_corp() stub removed from core/state.pyx
- All 312+ tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/23-phase-integration/23-01-SUMMARY.md`
</output>
