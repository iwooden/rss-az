---
phase: 20-nogil-mask-optimization
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - core/actions.pyx
autonomous: true

must_haves:
  truths:
    - "All 7 _fill_*_mask() functions have nogil in signature"
    - "_fill_mask_for_phase() dispatch function has nogil in signature"
    - "All existing tests pass"
    - "Benchmark shows no performance regression"
  artifacts:
    - path: "core/actions.pyx"
      provides: "nogil mask generation functions"
      contains: "noexcept nogil"
  key_links:
    - from: "core/actions.pyx"
      to: "mask generation callers"
      via: "nogil context compatibility"
      pattern: "_fill_.*_mask.*noexcept nogil"
---

<objective>
Add nogil to mask functions and verify performance

Purpose: Enable mask generation to run without holding the GIL, allowing future thread-level parallelization for AlphaZero self-play
Output: All mask functions marked nogil, tests pass, benchmark confirms no regression
</objective>

<execution_context>
@/home/icebreaker/.claude/get-shit-done/workflows/execute-plan.md
@/home/icebreaker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-nogil-mask-optimization/20-RESEARCH.md
@.planning/phases/20-nogil-mask-optimization/20-01-SUMMARY.md
@.planning/phases/20-nogil-mask-optimization/20-02-SUMMARY.md

# Files to modify
@core/actions.pyx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add nogil to all mask function signatures</name>
  <files>core/actions.pyx</files>
  <action>
Update each mask function signature to add `nogil`:

**Before (noexcept only):**
```cython
cdef void _fill_invest_mask(GameState state, ActionLayout* layout, float* mask) noexcept:
```

**After (noexcept nogil):**
```cython
cdef void _fill_invest_mask(GameState state, ActionLayout* layout, float* mask) noexcept nogil:
```

Functions to update (7 total):
1. `_fill_invest_mask` (~line 261)
2. `_fill_bid_mask` (~line 322)
3. `_fill_acquisition_mask` (~line 346)
4. `_fill_closing_mask` (~line 379)
5. `_fill_dividends_mask` (~line 387)
6. `_fill_issue_mask` (~line 414)
7. `_fill_ipo_mask` (~line 435)

Also update the dispatch function:
8. `_fill_mask_for_phase` (~line 482)

IMPORTANT: The signature must be `noexcept nogil` (both). The `noexcept` is critical to prevent GIL reacquisition for exception checking.

Note: If compilation fails, it means some cpdef method is still being called from within the function. Check the error message to identify which call needs to be replaced with a low-level accessor (may need to revisit Plan 02).
  </action>
  <verify>Build succeeds: `python setup.py build_ext --inplace`</verify>
  <done>All 8 functions have `noexcept nogil` in signature</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite</name>
  <files>None (verification only)</files>
  <action>
Run the complete test suite to verify nogil marking didn't break functionality.

```bash
pytest tests/ -v
```

All tests must pass. If any fail, investigate:
1. Check if test is related to mask generation
2. Verify the low-level accessor produces same results as cpdef method
3. Check for off-by-one errors in offset calculations
  </action>
  <verify>`pytest tests/ -v` shows all tests pass</verify>
  <done>All existing tests pass with nogil mask functions</done>
</task>

<task type="auto">
  <name>Task 3: Run benchmark and verify no regression</name>
  <files>None (verification only)</files>
  <action>
Run the benchmark to measure games per minute:

```bash
python setup.py benchmark --num-games=1000 --num-players=3
```

Record the games/minute value. Compare to baseline (from before Phase 20).

Expected outcome:
- Performance should be STABLE or IMPROVED (not degraded)
- The nogil marking itself doesn't parallelize yet, but proves the code can run without GIL
- Any regression would indicate a problem with the accessor implementations

If performance is significantly worse (>10% degradation):
1. Check for unnecessary pointer/offset recomputation in hot paths
2. Verify inline functions are actually being inlined (check generated C code if needed)
3. Consider caching offsets at function entry vs recomputing per-iteration
  </action>
  <verify>Benchmark completes, games/minute is stable or improved vs baseline</verify>
  <done>Benchmark shows no performance regression, nogil optimization complete</done>
</task>

</tasks>

<verification>
- Build: `python setup.py build_ext --inplace` completes without errors
- Tests: `pytest tests/ -v` all pass
- Signatures: `grep -n "noexcept nogil" core/actions.pyx | wc -l` should show 8+ matches (the 7 mask functions plus dispatch)
- Benchmark: `python setup.py benchmark` shows stable games/minute
</verification>

<success_criteria>
1. _fill_invest_mask has `noexcept nogil` signature
2. _fill_bid_mask has `noexcept nogil` signature
3. _fill_acquisition_mask has `noexcept nogil` signature
4. _fill_closing_mask has `noexcept nogil` signature
5. _fill_dividends_mask has `noexcept nogil` signature
6. _fill_issue_mask has `noexcept nogil` signature
7. _fill_ipo_mask has `noexcept nogil` signature
8. _fill_mask_for_phase has `noexcept nogil` signature
9. All existing tests pass
10. Benchmark shows games/minute is stable or improved (no >10% regression)
</success_criteria>

<output>
After completion, create `.planning/phases/20-nogil-mask-optimization/20-03-SUMMARY.md`
</output>
