---
phase: 14-flow-integration
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_acquisition.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Tests verify receivership auto-buy for affordable FI offers"
    - "Tests verify receivership auto-pass for unaffordable FI offers"
    - "Tests verify receivership skips non-FI offers"
    - "Tests verify receivership corps cannot sell (RECV-02)"
  artifacts:
    - path: "tests/test_acquisition.py"
      provides: "TestReceivershipAutoBuy class"
      contains: "class TestReceivershipAutoBuy"
  key_links:
    - from: "test_receivership_auto_buys_affordable_fi"
      to: "_execute_receivership_fi_buy"
      via: "setup_acquisition_phase_py triggers auto-buy"
      pattern: "has_acquisition_company.*True"
    - from: "test_receivership_cannot_sell"
      to: "_get_corp_president returns -1"
      via: "RECV-02 enforcement"
      pattern: "get_offer_count.*== 0"
---

<objective>
Add missing receivership auto-buy tests to close verification gaps.

Purpose: The receivership auto-buy implementation exists and is correct (verified via code inspection), but automated regression tests are missing. These tests ensure future changes don't break receivership behavior.

Output: TestReceivershipAutoBuy class in tests/test_acquisition.py with 4 test methods covering RECV-01, RECV-02, RECV-03.
</objective>

<execution_context>
@/home/icebreaker/.claude/get-shit-done/workflows/execute-plan.md
@/home/icebreaker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-flow-integration/14-VERIFICATION.md

# Implementation reference
@phases/acquisition.pyx (lines 458-473 for receivership check, lines 791-806 for _execute_receivership_fi_buy)

# Existing test patterns
@tests/test_acquisition.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add TestReceivershipAutoBuy class</name>
  <files>tests/test_acquisition.py</files>
  <action>
Add a new TestReceivershipAutoBuy class to tests/test_acquisition.py with 4 test methods:

1. **test_receivership_auto_buys_affordable_fi** (RECV-01, RECV-03):
   - Create GameState, initialize game
   - Transfer company 0 to FI: `COMPANIES[0].transfer_to_fi(gs)`
   - Make corp 0 active and set cash to 50000 (enough for any face value)
   - Put corp 0 in receivership: `CORPS[CORP_NAMES[0]].set_in_receivership(gs, True)`
   - Call `setup_acquisition_phase_py(gs)` to generate offers and trigger auto-buy
   - Assert corp 0 has_acquisition_company(gs, 0) is True (auto-buy executed)
   - Assert FI.get_cash(gs) increased by face_value
   - Assert corp 0's cash decreased by face_value
   - Assert get_offer_count(gs) == 0 OR acq_active_corp == -1 (offer was processed)

2. **test_receivership_skips_unaffordable_fi** (RECV-03 auto-pass):
   - Create GameState, initialize game
   - Transfer company 0 to FI
   - Make corp 0 active and set cash to 1 (not enough for face value)
   - Put corp 0 in receivership
   - Record FI cash before
   - Call setup_acquisition_phase_py(gs)
   - Assert corp 0 does NOT have_acquisition_company(gs, 0) (no buy)
   - Assert FI cash unchanged (no transfer)
   - Assert company 0 still owned by FI

3. **test_receivership_skips_non_fi_offers** (RECV-03 auto-pass for non-FI):
   - Create GameState, initialize game
   - Transfer company 0 to player 0: `COMPANIES[0].transfer_to_player(gs, 0)`
   - Make corp 0 active and set cash to 50000
   - Put corp 0 in receivership
   - Note: A receivership corp cannot be president of anything, so no player-president check needed
   - Call setup_acquisition_phase_py(gs)
   - Assert corp 0 does NOT have_acquisition_company(gs, 0)
   - Assert get_offer_count(gs) == 0 (no offers for receivership buying from player)

4. **test_receivership_cannot_sell** (RECV-02):
   - Create GameState, initialize game
   - Make corp 0 active, give it a company: `COMPANIES[0].transfer_to_corp(gs, 0)`
   - Put corp 0 in receivership
   - Make corp 1 active with cash, make player 0 president of corp 1
   - Call setup_acquisition_phase_py(gs)
   - Assert no offers exist where corp 0 is the seller
   - Verify by checking get_offer_count(gs) == 0 (since corp 0 is only potential seller but is in receivership)

Pattern to follow: Match existing test class structure (TestZoneMerging, TestPhaseFlow) with docstrings referencing requirement IDs.

Key imports already present: CORPS, COMPANIES, FI, PLAYERS, TURN, setup_acquisition_phase_py, get_offer_count, CORP_NAMES
  </action>
  <verify>
Run: `python setup.py build_ext --inplace && pytest tests/test_acquisition.py::TestReceivershipAutoBuy -v`

Expected: All 4 tests pass
  </verify>
  <done>
TestReceivershipAutoBuy class exists with 4 passing test methods:
- test_receivership_auto_buys_affordable_fi
- test_receivership_skips_unaffordable_fi
- test_receivership_skips_non_fi_offers
- test_receivership_cannot_sell
  </done>
</task>

</tasks>

<verification>
1. Run receivership tests: `pytest tests/test_acquisition.py::TestReceivershipAutoBuy -v` - all 4 pass
2. Run full acquisition tests: `pytest tests/test_acquisition.py -v` - all tests pass (no regressions)
3. Run full test suite: `pytest tests/` - all tests pass
4. Grep for class: `grep -n "TestReceivershipAutoBuy" tests/test_acquisition.py` - class exists
</verification>

<success_criteria>
- [ ] TestReceivershipAutoBuy class added to tests/test_acquisition.py
- [ ] test_receivership_auto_buys_affordable_fi passes (RECV-01, RECV-03)
- [ ] test_receivership_skips_unaffordable_fi passes (RECV-03)
- [ ] test_receivership_skips_non_fi_offers passes (RECV-03)
- [ ] test_receivership_cannot_sell passes (RECV-02)
- [ ] Full test suite passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/14-flow-integration/14-04-SUMMARY.md`
</output>
