---
phase: 05-presidency-bankruptcy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - phases/invest.pyx
autonomous: true

must_haves:
  truths:
    - "Corporation goes bankrupt when price drops to index 0"
    - "Bankruptcy removes all companies from game"
    - "Bankruptcy returns all shares to unissued"
    - "Bankruptcy clears corp cash and resets to inactive"
    - "Market space freed for future use"
  artifacts:
    - path: "phases/invest.pyx"
      provides: "_execute_bankruptcy() helper and sell handler integration"
      exports: ["_execute_bankruptcy"]
  key_links:
    - from: "_handle_sell_share"
      to: "_execute_bankruptcy"
      via: "conditional call when price_index == 0"
      pattern: "if.*get_price_index.*== 0.*_execute_bankruptcy"
---

<objective>
Implement corporation bankruptcy procedure triggered by share price dropping to 0.

Purpose: When a sell action moves a corp's price to index 0, the corp immediately goes bankrupt. This is a complete reset: companies removed, shares returned to unissued, cash returned to bank, market space freed, corp inactive and available for future IPO.

Output: Modified `phases/invest.pyx` with `_execute_bankruptcy()` helper and integration into `_handle_sell_share()`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase-specific context
@.planning/phases/05-presidency-bankruptcy/05-CONTEXT.md
@.planning/phases/05-presidency-bankruptcy/05-RESEARCH.md

# Prior phase for handler structure
@.planning/phases/04-share-trading/04-01-SUMMARY.md

# Source files
@phases/invest.pyx
@entities/corp.pyx
@entities/company.pyx
@entities/player.pyx
@entities/market.pyx
@core/data.pyx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement bankruptcy procedure helper</name>
  <files>phases/invest.pyx</files>
  <action>
Add `_execute_bankruptcy()` cdef helper function to `phases/invest.pyx`.

Import requirements at top of file:
- Import `company` module: `from entities import company as company_module`
- Import `get_corp_share_count` from `core.data`: add to existing cimport

Function signature: `cdef void _execute_bankruptcy(GameState state, int corp_id) noexcept:`

Implementation steps (per CONTEXT.md order):

1. Get corp entity: `corp = corp_module.CORPS[CORP_NAMES[corp_id]]`

2. Remove all owned companies from game:
   ```
   for company_id in range(GameConstants.NUM_COMPANIES):
       if corp.owns_company(state, company_id):
           company_module.COMPANIES[company_id].remove_from_game(state)
           corp.set_owns_company(state, company_id, False)
   ```

3. Return all shares to unissued - clear player shares first:
   ```
   for player_id in range(state._num_players):
       player_module.PLAYERS[player_id].set_shares(state, corp_id, 0)
       player_module.PLAYERS[player_id].set_president_of(state, corp_id, False)
   ```

   **Note:** `set_president_of(state, corp_id, False)` only affects the specific `corp_id` parameter for that player. It does NOT affect presidency of other corporations. This is correct behavior - we only clear presidency for the bankrupt corp.

4. Reset corp share counts:
   ```
   corp.set_unissued_shares(state, get_corp_share_count(corp_id))
   corp.set_issued_shares(state, 0)
   corp.set_bank_shares(state, 0)
   ```

5. Return money to bank (clear corp cash):
   `corp.set_cash(state, 0)`

6. Free market space if needed (the sell already freed old space, but verify):
   ```
   current_index = corp.get_price_index(state)
   if current_index > 0:
       market_module.MARKET.set_space_available(state, current_index, True)
   ```

7. Deactivate corp and clear remaining state:
   ```
   corp.set_active(state, False)
   corp.set_price_index(state, 0)
   corp.set_in_receivership(state, False)
   corp.set_income(state, 0)
   corp.set_stars(state, 0)
   corp.set_acquisition_proceeds(state, 0)
   ```

   **Note:** Setting `corp.set_active(state, False)` automatically excludes this corp from action masks. The existing action mask logic in `_compute_invest_mask()` already checks `corp.is_active(state)` before allowing buy/sell actions. Bankrupt corps will not appear in any trading masks.

8. Clear acquisition company flags:
   ```
   for company_id in range(GameConstants.NUM_COMPANIES):
       corp.set_acquisition_company(state, company_id, False)
   ```

Declare all cdef variables at function start (Cython pattern):
```
cdef int company_id, player_id, current_index
cdef object corp
```
  </action>
  <verify>
Build succeeds: `python setup.py build_ext --inplace`

Verify action mask excludes inactive corps (code inspection): The `_compute_invest_mask()` function should check `corp.is_active(state)` before enabling buy/sell actions.
  </verify>
  <done>
`_execute_bankruptcy()` function exists in invest.pyx, handles all 6 INV-22 through INV-27 requirements. Bankrupt corp excluded from action masks via existing `is_active()` check.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate bankruptcy check into sell handler</name>
  <files>phases/invest.pyx</files>
  <action>
Modify `_handle_sell_share()` to call `_execute_bankruptcy()` when price drops to 0.

Current sell handler flow (per 04-01-SUMMARY):
1. Get current price, transfer money, transfer share
2. Move price down
3. Track round-trip, update net worth
4. Reset passes, advance player

New flow - insert bankruptcy check after price movement (step 2), before tracking:

After the line `corp.set_price_index(state, new_index)`:
```
# Check for bankruptcy (INV-22)
if new_index == 0:
    _execute_bankruptcy(state, corp_id)
    # Update net worth even after bankruptcy (player received cash)
    player_module.PLAYERS[player_id].update_net_worth(state)
    # Reset consecutive passes (INV-02)
    turn_module.TURN.clear_consecutive_passes(state)
    # Advance active player
    _advance_active_player(state)
    return  # Skip receivership/presidency checks - corp is gone
```

The existing code after price movement handles the non-bankruptcy case:
```
if new_index > 0:  # Occupy new space (unless bankruptcy)
    market_module.MARKET.set_space_available(state, new_index, False)
```

This existing check is correct - bankruptcy case now returns early, so the `if new_index > 0` handles non-bankruptcy price movement.

Remove the comment "Note: new_index == 0 means bankruptcy - Phase 5 handles the procedure" since we now handle it.
  </action>
  <verify>
Build succeeds: `python setup.py build_ext --inplace`

Quick test - existing share trading tests still pass:
`pytest tests/test_share_trading.py -v`
  </verify>
  <done>
Sell handler calls `_execute_bankruptcy()` when price reaches 0, then returns early. Existing share trading tests pass.
  </done>
</task>

</tasks>

<verification>
1. Build succeeds: `python setup.py build_ext --inplace`
2. Existing tests pass: `pytest tests/test_share_trading.py -v`
3. Code inspection: `_execute_bankruptcy()` exists and is called from `_handle_sell_share()`
4. Code inspection: Verify `_compute_invest_mask()` checks `is_active()` before enabling corp actions
</verification>

<success_criteria>
- INV-22: Bankruptcy triggered when price index = 0 (checked in sell handler)
- INV-23: All companies removed from game (loop through owned companies, call remove_from_game)
- INV-24: All shares returned to unissued (clear player shares, reset corp share counts)
- INV-25: Corp cash set to 0
- INV-26: Market space freed (handled by existing sell logic + bankruptcy cleanup)
- INV-27: Corp set inactive, available for future IPO (set_active(False), all state reset)
- Bankrupt corp excluded from action masks (via existing is_active() check)
</success_criteria>

<output>
After completion, create `.planning/phases/05-presidency-bankruptcy/05-01-SUMMARY.md`
</output>
