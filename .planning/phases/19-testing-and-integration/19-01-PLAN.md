---
phase: 19-testing-and-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phases/test_closing.py
autonomous: true

must_haves:
  truths:
    - "Empty offers scenario transitions directly to INCOME"
    - "All-pass scenario triggers mandatory close before transition"
    - "Multi-close cascade handles sequential closes correctly"
    - "Corp last-company dynamic validation works across closes"
    - "Player count variations work for edge cases"
  artifacts:
    - path: "tests/phases/test_closing.py"
      provides: "Edge case tests for CLOSING phase"
      contains: "TestClosingEdgeCases"
  key_links:
    - from: "tests/phases/test_closing.py"
      to: "phases/closing.py"
      via: "apply_closing_auto_py, apply_closing_action_py"
      pattern: "apply_closing.*_py"
---

<objective>
Add comprehensive edge case tests for CLOSING phase covering empty offers, all-pass scenarios, multi-close cascades, and player count variations.

Purpose: Ensure CLOSING phase handles boundary conditions and rare game states correctly.
Output: Extended test_closing.py with TestClosingEdgeCases class.
</objective>

<execution_context>
@/home/icebreaker/.claude/get-shit-done/workflows/execute-plan.md
@/home/icebreaker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-testing-and-integration/19-CONTEXT.md
@.planning/phases/19-testing-and-integration/19-RESEARCH.md
@tests/phases/test_closing.py
@tests/phases/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add edge case tests for empty offers and all-pass scenarios</name>
  <files>tests/phases/test_closing.py</files>
  <action>
Add a new TestClosingEdgeCases class at the end of test_closing.py with the following tests:

1. test_no_close_offers_direct_transition:
   - Set up state where ALL companies have positive adjusted income (low CoO level)
   - Enter CLOSING phase and call apply_closing_auto_py
   - Verify: No offers generated, phase transitions directly to INVEST
   - Use assert_invariants before and after

2. test_all_pass_triggers_mandatory_close:
   - Set up player with negative-income company and low cash (triggers mandatory close)
   - Enter CLOSING phase, generate offers
   - Pass on all offers (use apply_closing_action_py with ACTION_PASS_PY)
   - Verify: After all offers passed, mandatory close triggers and closes company
   - Verify: Phase transitions to INVEST

3. test_all_pass_no_mandatory_close_needed:
   - Set up player with negative-income company but high cash (no mandatory close needed)
   - Enter CLOSING phase, generate offers
   - Pass on all offers
   - Verify: Company still exists (not closed)
   - Verify: Phase transitions to INVEST

Use existing fixtures (closing_offer_state) and helpers (assert_invariants) from conftest.py.
Use docstrings with requirement traceability (e.g., "Edge case: Empty offers").
  </action>
  <verify>pytest tests/phases/test_closing.py::TestClosingEdgeCases -v</verify>
  <done>3 edge case tests pass: empty offers, all-pass with mandatory close, all-pass without mandatory close</done>
</task>

<task type="auto">
  <name>Task 2: Add multi-close cascade and player count tests</name>
  <files>tests/phases/test_closing.py</files>
  <action>
Add more tests to TestClosingEdgeCases class:

4. test_multi_close_cascade_js_bonus:
   - Set up player with multiple negative-income companies
   - Activate Junkyard Scrappers (corp 0) with starting cash
   - Enter CLOSING phase, accept multiple close offers sequentially
   - Verify: Each close triggers JS bonus (2x printed income)
   - Verify: Total JS bonus equals sum of 2x income for all closed companies

5. test_corp_last_company_dynamic_invalidation:
   - Set up corp with exactly 2 negative-income companies
   - Player is president of corp
   - Enter CLOSING phase
   - Accept first close offer (corp now has 1 company)
   - Verify: Second offer is automatically skipped (corp last-company rule)
   - Verify: Phase transitions to INVEST

6. test_closing_edge_cases_with_six_players:
   - Use @pytest.mark.parametrize("num_players", [3, 6])
   - Set up state with negative-income companies for multiple players
   - Verify CLOSING phase works correctly for both player counts
   - Use assert_invariants throughout

Import any needed constants (ACTION_CLOSE_PY, ACTION_PASS_PY) at top of file if not already imported.
Follow existing test patterns from TestCloseActions and TestMandatoryClose.
  </action>
  <verify>pytest tests/phases/test_closing.py::TestClosingEdgeCases -v</verify>
  <done>6 edge case tests total pass in TestClosingEdgeCases class</done>
</task>

<task type="auto">
  <name>Task 3: Verify all tests pass with no regressions</name>
  <files>tests/phases/test_closing.py</files>
  <action>
Run the full test_closing.py test suite to verify:
1. All 42 existing tests still pass
2. All 6 new edge case tests pass
3. No regressions introduced

Run pytest with -v flag to see individual test results.
If any tests fail, diagnose and fix without changing the behavior of existing tests.
  </action>
  <verify>pytest tests/phases/test_closing.py -v && echo "All tests pass"</verify>
  <done>48 total tests pass (42 existing + 6 new edge cases)</done>
</task>

</tasks>

<verification>
1. pytest tests/phases/test_closing.py -v shows 48+ passing tests
2. TestClosingEdgeCases class exists with 6 tests
3. No test failures or regressions
</verification>

<success_criteria>
- [ ] Empty offers scenario test passes
- [ ] All-pass with mandatory close test passes
- [ ] All-pass without mandatory close test passes
- [ ] Multi-close cascade with JS bonus test passes
- [ ] Corp last-company dynamic invalidation test passes
- [ ] Player count parameterized test passes for 3 and 6 players
- [ ] All 42 existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-testing-and-integration/19-01-SUMMARY.md`
</output>
