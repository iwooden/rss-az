---
phase: 19-testing-and-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_integration.py
autonomous: true

must_haves:
  truths:
    - "ACQUISITION -> CLOSING -> INCOME flow works with offers"
    - "ACQUISITION -> CLOSING -> INCOME flow works with accepts"
    - "ACQUISITION -> CLOSING -> INCOME flow works with passes + mandatory close"
    - "Full turn cycle with CLOSING maintains invariants"
    - "Multiple player counts work for integration flow"
  artifacts:
    - path: "tests/test_integration.py"
      provides: "CLOSING phase integration tests"
      contains: "TestClosingIntegration"
  key_links:
    - from: "tests/test_integration.py"
      to: "phases/closing.py"
      via: "apply_closing_auto_py, apply_closing_action_py"
      pattern: "apply_closing.*_py"
    - from: "tests/test_integration.py"
      to: "phases/acquisition.py"
      via: "transition_to_closing_py"
      pattern: "transition_to_closing_py"
---

<objective>
Add integration tests for ACQUISITION -> CLOSING -> INCOME flow with scripted action sequences.

Purpose: Verify phase transitions work correctly across the full CLOSING lifecycle.
Output: Extended test_integration.py with TestClosingIntegration class.
</objective>

<execution_context>
@/home/icebreaker/.claude/get-shit-done/workflows/execute-plan.md
@/home/icebreaker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-testing-and-integration/19-CONTEXT.md
@.planning/phases/19-testing-and-integration/19-RESEARCH.md
@tests/test_integration.py
@tests/phases/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CLOSING integration test class with flow tests</name>
  <files>tests/test_integration.py</files>
  <action>
Add a new TestClosingIntegration class after TestAcquisitionIntegration with the following tests:

1. test_closing_with_no_offers_flow:
   - Start from ACQUISITION phase (after WRAP_UP)
   - Transition to CLOSING (no negative-income companies = no offers)
   - Verify: Direct transition to INVEST
   - Use assert_invariants at each step

2. test_closing_with_accept_flow:
   - Set up state with player owning negative-income company
   - Set up high CoO level to create negative income
   - Enter ACQUISITION -> transition to CLOSING
   - Accept the close offer
   - Verify: Company removed, JS bonus paid (if JS active)
   - Verify: Phase transitions to INVEST
   - Use apply_closing_action_py and ACTION_CLOSE_PY

3. test_closing_with_pass_and_mandatory_close_flow:
   - Set up player with negative-income company and low cash
   - Enter ACQUISITION -> transition to CLOSING
   - Pass on the close offer
   - Verify: Mandatory close triggers, company removed
   - Verify: Phase transitions to INVEST

Import needed functions:
- from phases.closing import apply_closing_auto_py, apply_closing_action_py
- from phases.acquisition import transition_to_closing_py
- from core.actions import ACTION_CLOSE_PY, ACTION_PASS_PY

Follow existing TestAcquisitionIntegration patterns.
  </action>
  <verify>pytest tests/test_integration.py::TestClosingIntegration -v</verify>
  <done>3 CLOSING integration tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Add full turn cycle and player count integration tests</name>
  <files>tests/test_integration.py</files>
  <action>
Add more tests to TestClosingIntegration class:

4. test_full_turn_cycle_with_closing_offers:
   - Start fresh game
   - Give player a company with negative income at high CoO
   - Execute full turn: INVEST (all pass) -> WRAP_UP -> ACQUISITION -> CLOSING -> INVEST
   - At CLOSING, accept the close offer
   - Verify: Turn number incremented after CLOSING -> INVEST
   - Use assert_invariants at each transition

5. test_closing_integration_player_counts:
   - Use @pytest.mark.parametrize("num_players", [3, 6])
   - Set up negative-income scenario for each player count
   - Execute ACQUISITION -> CLOSING -> INVEST flow
   - Verify invariants maintained throughout
   - Verify correct phase transitions

6. test_acquisition_accept_then_closing_accept:
   - Set up state with acquisition offer AND close offer potential
   - Accept acquisition offer in ACQUISITION phase
   - Transition to CLOSING
   - Accept close offer in CLOSING phase
   - Verify: Both actions complete correctly
   - Verify: Final state in INVEST with new turn

Use scripted action sequences (not random) per CONTEXT.md guidance.
Use apply_action_and_verify helper where appropriate.
  </action>
  <verify>pytest tests/test_integration.py::TestClosingIntegration -v</verify>
  <done>6 CLOSING integration tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Verify all integration tests pass</name>
  <files>tests/test_integration.py</files>
  <action>
Run the full test_integration.py test suite to verify:
1. All 19 existing integration tests still pass
2. All 6 new CLOSING integration tests pass
3. No regressions introduced

Run pytest with -v flag to see individual test results.
If any tests fail, diagnose and fix without changing the behavior of existing tests.
  </action>
  <verify>pytest tests/test_integration.py -v && echo "All tests pass"</verify>
  <done>25 total tests pass (19 existing + 6 new CLOSING integration tests)</done>
</task>

</tasks>

<verification>
1. pytest tests/test_integration.py -v shows 25+ passing tests
2. TestClosingIntegration class exists with 6 tests
3. No test failures or regressions
</verification>

<success_criteria>
- [ ] CLOSING with no offers flow test passes
- [ ] CLOSING with accept flow test passes
- [ ] CLOSING with pass and mandatory close flow test passes
- [ ] Full turn cycle with CLOSING offers test passes
- [ ] Player count parameterized integration test passes
- [ ] Acquisition accept then CLOSING accept test passes
- [ ] All 19 existing integration tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-testing-and-integration/19-02-SUMMARY.md`
</output>
