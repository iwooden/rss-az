---
phase: 15-testing
plan: 03
type: execute
wave: 2
depends_on: [15-01, 15-02]
files_modified:
  - tests/test_integration.py
autonomous: true

must_haves:
  truths:
    - "Integration tests verify INVEST->WRAP_UP->ACQUISITION->INVEST flow"
    - "Integration tests maintain invariants throughout acquisition actions"
    - "Tests verify zone merging happens at correct phase boundary"
  artifacts:
    - path: "tests/test_integration.py"
      provides: "Cross-phase integration tests including ACQUISITION"
      contains: "class TestAcquisitionIntegration"
  key_links:
    - from: "tests/test_integration.py"
      to: "phases/acquisition.pyx"
      via: "full driver flow"
      pattern: "DRIVER.apply_action"
---

<objective>
Add ACQUISITION phase integration tests to test_integration.py (TEST-06).

Purpose: Verify ACQUISITION phase integrates correctly with INVEST and WRAP_UP phases, maintaining game invariants throughout multi-phase sequences.

Output: TestAcquisitionIntegration class with end-to-end flow tests.
</objective>

<execution_context>
@/home/icebreaker/.claude/get-shit-done/workflows/execute-plan.md
@/home/icebreaker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-testing/15-CONTEXT.md
@.planning/phases/15-testing/15-RESEARCH.md
@.planning/phases/15-testing/15-01-SUMMARY.md
@.planning/phases/15-testing/15-02-SUMMARY.md
@tests/test_integration.py (after 15-01 moves it to root)
@tests/phases/conftest.py
@phases/acquisition.pyx
@core/driver.pyx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ACQUISITION integration test class</name>
  <files>tests/test_integration.py</files>
  <action>
Add TestAcquisitionIntegration class to test_integration.py (now at tests/ root after Plan 15-01).

Follow existing patterns from TestInvestIntegration and TestBidIntegration classes.

**test_wrap_up_to_acquisition_maintains_invariants:**
- Start in INVEST phase
- Pass all players to trigger WRAP_UP
- Verify WRAP_UP transitions to ACQUISITION
- Verify invariants maintained throughout
- Use assert_invariants() from conftest.py

**test_acquisition_to_invest_new_turn:**
- Set up game state in ACQUISITION phase with no offers
- Call transition_to_closing_py() or let phase complete naturally
- Verify transitions to INVEST
- Verify turn number incremented
- Verify invariants maintained

**test_full_turn_cycle_with_acquisition:**
- Start fresh game
- Pass all players (triggers WRAP_UP -> ACQUISITION)
- Acquisition completes (no offers in fresh game)
- Back to INVEST, turn 2
- Pass all players again
- Verify turn 3, invariants maintained throughout

Use apply_action_and_verify() for all actions to ensure invariants checked after each step.
  </action>
  <verify>
pytest tests/test_integration.py::TestAcquisitionIntegration -v
All 3+ tests pass
  </verify>
  <done>
TestAcquisitionIntegration class added with phase transition tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add acquisition action integration tests</name>
  <files>tests/test_integration.py</files>
  <action>
Add tests verifying ACQUISITION actions maintain invariants when executed through full driver flow.

**test_acquisition_accept_maintains_invariants:**
- Set up state with valid acquisition offer (player private -> corp)
- Enter ACQUISITION phase via WRAP_UP
- Apply acquisition accept action through DRIVER.apply_action
- Verify invariants maintained
- Verify company in acquisition zone
- Verify cash transferred correctly

**test_acquisition_pass_maintains_invariants:**
- Set up state with valid acquisition offer
- Enter ACQUISITION phase
- Apply pass action through driver
- Verify invariants maintained
- Verify offer advances or phase completes

**test_multiple_acquisitions_maintain_invariants:**
- Set up state with multiple offers
- Accept some, pass some
- Verify invariants after each action
- Verify zone merging at phase end

For these tests, use direct state manipulation to set up offers (like test_acquisition.py does), then use driver for action execution to test full integration path.

Import get_action_layout and get_valid_action_mask to find valid acquisition actions in mask.
  </action>
  <verify>
pytest tests/test_integration.py::TestAcquisitionIntegration -v
All tests pass, invariants verified after each action
  </verify>
  <done>
Integration tests verify ACQUISITION actions maintain invariants through full driver flow.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add zone merge integration test</name>
  <files>tests/test_integration.py</files>
  <action>
Add test verifying zone merging happens correctly at phase boundary.

**test_zone_merge_at_phase_transition:**
- Set up state with:
  - Player 0 has acquisition_proceeds from selling
  - Corp 0 has acquisition_proceeds from selling
  - Corp 0 has company in acquisition zone (not yet merged)
- Enter ACQUISITION phase, complete it (all offers processed)
- Verify at phase transition:
  - Player proceeds merged to cash
  - Corp proceeds merged to cash
  - Acquisition companies merged to owned_companies
  - All acquisition zones cleared

This test verifies FLOW-03 and FLOW-04 through the full driver path, not just calling merge functions directly.

Use assert_invariants() after transition to verify game state consistency.
  </action>
  <verify>
pytest tests/test_integration.py::TestAcquisitionIntegration -v
Zone merge test passes, all zones properly merged
  </verify>
  <done>
TEST-06 requirement satisfied: Integration tests cover INVEST->WRAP_UP->ACQUISITION flow with invariant verification.
  </done>
</task>

</tasks>

<verification>
1. Integration tests exist:
   - TestAcquisitionIntegration class in tests/test_integration.py
   - At least 5 tests covering phase transitions and action execution

2. Invariants verified:
   - Each test uses assert_invariants() from conftest.py
   - Tests cover: phase transitions, accept actions, pass actions, zone merging

3. All tests pass:
   - pytest tests/test_integration.py -v shows green for all integration tests
   - No existing tests broken
</verification>

<success_criteria>
- TEST-06 satisfied: Integration tests verify INVEST->WRAP_UP->ACQUISITION->CLOSING (INVEST) flow
- Invariants maintained throughout multi-phase sequences
- Zone merging verified at correct boundary
- pytest tests/ passes with no failures
</success_criteria>

<output>
After completion, create `.planning/phases/15-testing/15-03-SUMMARY.md`
</output>
