---
phase: 15-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phases/test_acquisition.py
autonomous: true

must_haves:
  truths:
    - "Validation boundary tests verify exact-threshold behavior"
    - "Edge case tests verify empty FI, no offers, all receivership scenarios"
    - "Each validation rule (VALID-01 through VALID-06) has boundary tests"
    - "Same-president constraint explicitly tested"
  artifacts:
    - path: "tests/phases/test_acquisition.py"
      provides: "Validation and edge case tests"
      contains: "class TestEdgeCases"
  key_links:
    - from: "tests/phases/test_acquisition.py"
      to: "phases/acquisition.pyx"
      via: "validation through action handlers"
      pattern: "apply_acquisition_action_py"
---

<objective>
Add validation boundary tests and edge case tests (TEST-03, TEST-07).

Purpose: Ensure validation rules correctly handle boundary conditions (exact cash, exact company counts) and edge cases (empty states, unusual configurations).

Output: TestValidation class with boundary tests, TestEdgeCases class with comprehensive empty-state and corner-case tests.
</objective>

<execution_context>
@/home/icebreaker/.claude/get-shit-done/workflows/execute-plan.md
@/home/icebreaker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-testing/15-CONTEXT.md
@.planning/phases/15-testing/15-RESEARCH.md
@tests/phases/test_acquisition.py (after 15-01 moves it here)
@tests/phases/conftest.py
@phases/acquisition.pyx
@core/data.pyx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add validation boundary tests</name>
  <files>tests/phases/test_acquisition.py</files>
  <action>
Add boundary condition tests to TestValidation class. Each test should verify exact threshold behavior.

**VALID-01 boundaries (price range):**
- test_price_at_low_boundary: Price exactly at low_price succeeds
- test_price_at_high_boundary: Price exactly at high_price succeeds
- test_price_one_below_low_fails: Price = low_price - 1 fails
- test_price_one_above_high_fails: Price = high_price + 1 fails

**VALID-02 boundaries (sufficient cash):**
- test_exact_cash_for_price_succeeds: Corp has exactly the price amount, action succeeds
- test_one_dollar_short_fails: Corp has price - 1, action fails (or offer not generated)

Note: VALID-02 is enforced at offer generation time (insufficient cash = no offer). Test should verify no offer generated when cash insufficient, rather than action rejection.

**VALID-03 boundaries (seller keeps >= 1 company):**
- test_seller_with_two_companies_can_sell_one: Seller has 2 companies, sell 1 succeeds
- test_seller_with_one_company_cannot_sell: Seller has exactly 1 company, selling it fails
- test_seller_with_one_owned_one_acquisition_can_sell: Seller has 1 owned + 1 in acquisition zone, can sell the owned one

For VALID-03, need to set up corp-to-corp scenario (same president required).

**VALID-04 and VALID-05:** Already have tests. Add one boundary test:
- test_company_in_acquisition_zone_blocks_offer: Put company in acquisition zone before setup, verify no offer generated

Use get_company_low_price() and get_company_face_value() from core.data to get actual price values for assertions.
  </action>
  <verify>
pytest tests/phases/test_acquisition.py::TestValidation -v
All validation tests pass including new boundary tests
  </verify>
  <done>
TestValidation covers all VALID-01 through VALID-06 with boundary conditions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add edge case tests</name>
  <files>tests/phases/test_acquisition.py</files>
  <action>
Create or extend TestEdgeCases class (or add to existing test classes as appropriate).

**Empty state tests:**
- test_no_active_corps_no_offers: No corps active, verify 0 offers
- test_empty_fi_no_fi_offers: FI owns no companies, verify no FI offers (other offers may exist)
- test_no_player_privates_no_private_offers: Players own no private companies, verify no private offers
- test_no_corp_companies_no_corp_corp_offers: Corps own no companies, verify no corp-to-corp offers

**All receivership tests:**
- test_all_corps_receivership_auto_processes: Multiple receivership corps, verify auto-buy happens for affordable FI
- test_receivership_corp_exhausts_all_fi: Receivership corp buys all affordable FI companies in sequence

**Configuration edge cases:**
- test_single_corp_no_corp_corp_offers: Only one corp active, can't have corp-to-corp (need buyer and seller)
- test_company_price_span_single_value: Company where low_price == high_price (if any exist), verify behavior

**Same-president explicit test (from CONTEXT.md specifics):**
- test_same_president_constraint_explicit: Setup scenario where same-president logic is the ONLY thing blocking an offer, verify no offer generated

Reference core/data.py for company price data. Company face values range from ~$10-140, price spans vary.
  </action>
  <verify>
pytest tests/phases/test_acquisition.py::TestEdgeCases -v (if new class)
or pytest tests/phases/test_acquisition.py -v (all tests pass)
  </verify>
  <done>
Edge case tests comprehensively cover empty states and unusual configurations (TEST-07 complete).
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify test coverage completeness</name>
  <files>tests/phases/test_acquisition.py</files>
  <action>
Review and verify all TEST requirements are covered:

1. Scan through test file and map each test to requirements:
   - TEST-01 (offer generation): TestOfferGeneration class
   - TEST-02 (action types): TestActionIntegration class (existing)
   - TEST-03 (validation): TestValidation class
   - TEST-04 (receivership): TestReceivershipAutoBuy class (existing)
   - TEST-05 (phase flow): TestPhaseFlow and TestZoneMerging classes (existing)
   - TEST-07 (edge cases): TestEdgeCases class (new)

2. Ensure each test method has requirement ID in docstring per CONTEXT.md pattern.

3. Add any missing coverage identified during review.

4. Run full test suite to verify no regressions.

Note: TEST-06 (integration tests) will be handled in Plan 15-03 which focuses on cross-phase flow.
  </action>
  <verify>
pytest tests/phases/test_acquisition.py -v
All tests pass, console output shows comprehensive coverage
  </verify>
  <done>
TEST-01, TEST-02, TEST-03, TEST-04, TEST-05, TEST-07 requirements satisfied by tests in test_acquisition.py.
  </done>
</task>

</tasks>

<verification>
1. Validation boundaries tested:
   - VALID-01: Price range boundaries (at low, at high, below low, above high)
   - VALID-02: Cash boundaries (exact, insufficient)
   - VALID-03: Company count boundaries (2->1 ok, 1->0 blocked, acquisition zone counts)
   - VALID-04, VALID-05: Existing tests + boundary additions

2. Edge cases covered:
   - Empty states (no corps, no FI, no privates, no corp companies)
   - Receivership scenarios
   - Same-president constraint explicit verification

3. All tests pass:
   - pytest tests/phases/test_acquisition.py -v shows green across all test classes
</verification>

<success_criteria>
- TEST-03 satisfied: All validation rules have boundary condition tests
- TEST-07 satisfied: Edge cases (empty states, all receivership) are tested
- Each test method has requirement ID in docstring
- pytest tests/ passes with no failures
</success_criteria>

<output>
After completion, create `.planning/phases/15-testing/15-02-SUMMARY.md`
</output>
