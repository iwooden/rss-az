---
phase: 15-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/phases/test_acquisition.py
  - tests/test_integration.py
autonomous: true

must_haves:
  truths:
    - "pytest tests/phases/test_acquisition.py passes"
    - "Offer priority tests verify OS-first ordering"
    - "Offer priority tests verify price-descending sorting"
    - "Offer priority tests verify same-president constraint"
    - "test_acquisition.py moved to tests/phases/"
    - "test_integration.py moved to tests/ root"
  artifacts:
    - path: "tests/phases/test_acquisition.py"
      provides: "ACQUISITION phase unit tests"
      contains: "class TestOfferGeneration"
    - path: "tests/test_integration.py"
      provides: "Cross-phase integration tests"
      contains: "class TestInvestIntegration"
  key_links:
    - from: "tests/phases/test_acquisition.py"
      to: "phases/acquisition.pyx"
      via: "import from phases.acquisition"
      pattern: "from phases.acquisition import"
---

<objective>
Reorganize test files per CONTEXT.md decisions and complete offer generation priority tests (TEST-01).

Purpose: Establish proper test organization and verify ACQUISITION offer generation produces correct priority ordering (OS-first, price-sorted, same-president constraints).

Output: Relocated test files with complete TestOfferGeneration class covering OFFER-01 through OFFER-05.
</objective>

<execution_context>
@/home/icebreaker/.claude/get-shit-done/workflows/execute-plan.md
@/home/icebreaker/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-testing/15-CONTEXT.md
@.planning/phases/15-testing/15-RESEARCH.md
@tests/test_acquisition.py
@tests/phases/test_integration.py
@tests/phases/conftest.py
@phases/acquisition.pyx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorganize test files</name>
  <files>
    tests/phases/test_acquisition.py
    tests/test_integration.py
  </files>
  <action>
Move test files per CONTEXT.md decisions:

1. Move `tests/test_acquisition.py` to `tests/phases/test_acquisition.py`
   - Use `git mv` to preserve history
   - Verify imports still work (they use absolute imports so should be fine)

2. Move `tests/phases/test_integration.py` to `tests/test_integration.py`
   - Use `git mv` to preserve history
   - Update any relative imports to absolute imports if needed

After moves, run `pytest tests/phases/test_acquisition.py -v` and `pytest tests/test_integration.py -v` to verify both still pass.
  </action>
  <verify>
pytest tests/phases/test_acquisition.py -v (all existing tests pass)
pytest tests/test_integration.py -v (all existing tests pass)
  </verify>
  <done>
Test files relocated, all existing tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Complete offer generation priority tests</name>
  <files>tests/phases/test_acquisition.py</files>
  <action>
Fill in TODO tests in TestOfferGeneration class. Each test should:
- Include requirement ID in docstring
- Use direct entity manipulation for setup
- Call setup_acquisition_phase_py() to generate offers
- Verify offer order using get_offer_at() and get_offer_count()

Tests to implement:

**test_os_fi_offers_first (OFFER-02):**
- Setup: FI owns company 0, OS (corp 2) active with $50000, non-OS corp (corp 0) active with $50000
- Make player 0 president of both corps
- Call setup_acquisition_phase_py()
- Verify first offer is from OS (corp_id == 2)

**test_corp_fi_sorted_by_price (OFFER-03):**
- Setup: FI owns company 0
- Make corp 0 active at price_index 15 (higher price), corp 1 active at price_index 10 (lower price)
- Both corps have $50000 cash
- Make player 0 president of both (skip OS for simplicity)
- Call setup_acquisition_phase_py()
- Get offers, verify higher-priced corp appears before lower-priced corp

**test_corp_corp_offers_same_president (OFFER-04):**
- Setup: Corp 0 owns company 0, Corp 1 active with $50000
- Player 0 is president of BOTH corp 0 and corp 1 (same president)
- Call setup_acquisition_phase_py()
- Verify offers exist (corp 1 can buy from corp 0)

**test_different_president_no_offers (OFFER-04 negative case):**
- Setup: Corp 0 owns company 0, Corp 1 active with $50000
- Player 0 is president of corp 0, Player 1 is president of corp 1 (different presidents)
- Call setup_acquisition_phase_py()
- Verify NO corp-to-corp offers generated (same-president constraint enforced)

**test_player_private_offers (OFFER-05):**
- Setup: Player 0 owns private company 0
- Corp 0 active with $50000, player 0 is president
- Call setup_acquisition_phase_py()
- Verify offer exists: corp 0 can buy player 0's private company

Use get_company_face_value() to find a company with appropriate price for corp cash. Company 0 has face value around $10-20.
  </action>
  <verify>
pytest tests/phases/test_acquisition.py::TestOfferGeneration -v
All 6+ tests pass (existing + new)
  </verify>
  <done>
TestOfferGeneration has complete coverage for OFFER-01 through OFFER-05 requirements.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add priority order sorting tests</name>
  <files>tests/phases/test_acquisition.py</files>
  <action>
Add tests to verify sorting within offer categories:

**test_fi_offers_sorted_by_corp_share_price (OFFER-03 detail):**
- Setup: FI owns companies 0 and 1
- Corp 0 at price_index 20, Corp 1 at price_index 10
- Both have sufficient cash, player 0 is president of both
- Verify offers for higher-priced corp appear before lower-priced corp (for SAME FI company)

**test_corp_corp_sorted_by_buyer_price_then_face_value (OFFER-04 detail):**
- Setup complex scenario with multiple corps and companies
- Verify sorting: buyer share price descending, then target face value ascending

**test_player_private_sorted_similarly (OFFER-05 detail):**
- Setup player with multiple private companies, multiple corps
- Verify sorting matches corp-to-corp pattern

These tests verify the implementation details of _collect_*_offers functions. If setup is too complex for reliable testing, add a comment explaining why and mark as tested-by-implementation (the sorting code is straightforward selection sort).
  </action>
  <verify>
pytest tests/phases/test_acquisition.py::TestOfferGeneration -v
All tests pass
  </verify>
  <done>
Offer generation tests comprehensively cover priority ordering (TEST-01 complete).
  </done>
</task>

</tasks>

<verification>
1. All test files in correct locations:
   - tests/phases/test_acquisition.py exists
   - tests/test_integration.py exists (at root)
   - tests/test_acquisition.py does NOT exist (moved)
   - tests/phases/test_integration.py does NOT exist (moved)

2. All tests pass:
   - pytest tests/ runs without failures
   - Specifically pytest tests/phases/test_acquisition.py::TestOfferGeneration -v shows all priority tests passing

3. No regressions:
   - Total test count same or higher than before
</verification>

<success_criteria>
- TEST-01 requirement satisfied: Unit tests verify offer generation produces correct priority ordering
- OFFER-01 through OFFER-05 all have dedicated test coverage
- Test file organization matches CONTEXT.md decisions
- pytest tests/ passes with no failures
</success_criteria>

<output>
After completion, create `.planning/phases/15-testing/15-01-SUMMARY.md`
</output>
