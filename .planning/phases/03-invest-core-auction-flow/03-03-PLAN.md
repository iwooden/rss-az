---
phase: 03-invest-core-auction-flow
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - tests/test_invest.py
  - tests/test_bid.py
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Pass action behavior verified with tests"
    - "Start auction behavior verified with tests"
    - "Leave auction behavior verified with tests"
    - "Raise bid behavior verified with tests"
    - "Auction resolution verified end-to-end"
    - "Turn order navigation verified across player counts"
  artifacts:
    - path: "tests/test_invest.py"
      provides: "INVEST phase test coverage"
      min_lines: 100
    - path: "tests/test_bid.py"
      provides: "BID phase test coverage"
      min_lines: 100
  key_links:
    - from: "tests/test_invest.py"
      to: "core/driver.pyx"
      via: "DRIVER.apply_action for action execution"
      pattern: "DRIVER\\.apply_action"
    - from: "tests/test_bid.py"
      to: "core/driver.pyx"
      via: "DRIVER.apply_action for action execution"
      pattern: "DRIVER\\.apply_action"
---

<objective>
Create comprehensive test coverage for INVEST and BID phase implementations.

Purpose: Verify all 19 requirements (INV-01 through INV-06, BID-01 through BID-12) are correctly implemented.
Output: Test files covering pass, auction start, leave, raise bid, and auction resolution.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-invest-core-auction-flow/03-01-SUMMARY.md
@.planning/phases/03-invest-core-auction-flow/03-02-SUMMARY.md

# Test patterns from Phase 2
@tests/test_driver.py
@tests/conftest.py

# Implementation files
@phases/invest.pyx
@phases/bid.pyx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create INVEST phase tests</name>
  <files>tests/test_invest.py</files>
  <action>
Create test file covering INV-01 through INV-06.

**Test structure (follow test_driver.py patterns):**
```python
"""Tests for INVEST phase actions."""
import pytest
from core.state import GameState
from core.driver import DRIVER
from core.actions import get_valid_action_mask, get_action_layout
from core.data import GamePhases
from entities.turn import TURN
from entities.player import PLAYERS

STATUS_OK = 0
```

**Fixtures:**
- `game_state(num_players=3, seed=42)` - Basic initialized state in INVEST phase
- `parametrize num_players [3, 4, 5, 6]` for player count tests

**Test classes and cases:**

**TestPassAction:**
- `test_pass_increments_consecutive_passes` (INV-01)
- `test_pass_advances_active_player` (INV-04)
- `test_pass_follows_turn_order` (INV-04a) - verify turn_order not player_id
- `test_all_players_pass_transitions_to_wrap_up` (INV-03)
- `test_non_pass_resets_consecutive_passes` (INV-02) - auction action should reset

**TestStartAuction:**
- `test_start_auction_sets_company` (INV-05)
- `test_start_auction_sets_price` (INV-05)
- `test_start_auction_sets_high_bidder` (INV-05)
- `test_start_auction_sets_starter` (INV-05)
- `test_start_auction_clears_passed_flags` (INV-05)
- `test_start_auction_transitions_to_bid_phase` (INV-06)
- `test_start_auction_advances_to_next_bidder`
- `test_start_auction_resets_consecutive_passes` (INV-02)

**TestMultiplePlayerCounts:**
- `test_pass_works_all_player_counts` (parametrized)
- `test_auction_works_all_player_counts` (parametrized)
- `test_wrap_up_triggers_at_correct_pass_count` (parametrized)

**Helper functions:**
- `get_first_valid_auction_action(state)` - find valid auction action index
- `apply_pass_to_all_players(state, num_players)` - helper for wrap_up test
  </action>
  <verify>
Tests run: `pytest tests/test_invest.py -v`
All tests pass
  </verify>
  <done>
- INV-01 verified: Pass increments consecutive_passes
- INV-02 verified: Non-pass resets consecutive_passes
- INV-03 verified: WRAP_UP transition after all pass
- INV-04 verified: Active player advances in turn order
- INV-04a verified: Turn order from one-hot vectors
- INV-05 verified: Auction state initialization
- INV-06 verified: Phase transition to BID
  </done>
</task>

<task type="auto">
  <name>Task 2: Create BID phase tests</name>
  <files>tests/test_bid.py</files>
  <action>
Create test file covering BID-01 through BID-12.

**Test structure:**
```python
"""Tests for BID_IN_AUCTION phase actions."""
import pytest
from core.state import GameState
from core.driver import DRIVER
from core.actions import get_valid_action_mask, get_action_layout
from core.data import GamePhases
from entities.turn import TURN
from entities.player import PLAYERS
from entities.company import COMPANIES
from entities.deck import DECK

STATUS_OK = 0
```

**Fixtures:**
- `auction_state()` - State with active auction (in BID phase)
  - Initialize game, apply first valid auction action
  - Return state with active auction

**Test classes and cases:**

**TestLeaveAuction:**
- `test_leave_sets_passed_flag` (BID-01)
- `test_leave_advances_to_next_bidder` (BID-02)
- `test_leave_skips_passed_players` (BID-02)
- `test_last_leaver_triggers_resolution` (BID-05)

**TestRaiseBid:**
- `test_raise_updates_price` (BID-03)
- `test_raise_updates_high_bidder` (BID-03)
- `test_raise_advances_to_next_bidder`
- `test_raise_bid_values_correct` - verify bid = face + amount + 1

**TestAuctionResolution:**
- `test_winner_pays_bid_price` (BID-06)
- `test_winner_receives_company` (BID-07)
- `test_auction_state_cleared` (BID-08)
- `test_new_company_drawn` (BID-09)
- `test_returns_to_invest_phase` (BID-10)
- `test_turn_goes_to_player_after_starter` (BID-11)
- `test_winner_net_worth_updated` (BID-12)

**TestFullAuctionCycle:**
- `test_complete_auction_cycle` - full flow: start -> bid/leave -> resolution -> back to INVEST
- `test_auction_with_immediate_resolution` - all others leave immediately
- `test_auction_with_multiple_raises` - several raises before resolution

**TestMultiplePlayerCounts:**
- `test_auction_works_all_player_counts` (parametrized)
- `test_bidder_rotation_correct_all_counts` (parametrized)

**Key verification patterns:**
- Check state before and after actions
- Verify cash changes match bid prices
- Verify company ownership transfers
- Verify turn order navigation is correct
  </action>
  <verify>
Tests run: `pytest tests/test_bid.py -v`
All tests pass
  </verify>
  <done>
- BID-01 verified: Leave sets passed flag
- BID-02 verified: Rotation skips passed players
- BID-03 verified: Raise updates price/bidder
- BID-05 verified: Resolution triggers with one bidder
- BID-06 verified: Winner pays
- BID-07 verified: Winner gets company
- BID-08 verified: Auction state cleared
- BID-09 verified: New company drawn
- BID-10 verified: Returns to INVEST
- BID-11 verified: Turn to player after starter
- BID-12 verified: Net worth updated
  </done>
</task>

</tasks>

<verification>
1. All tests pass: `pytest tests/test_invest.py tests/test_bid.py -v`
2. Full test suite still passes: `pytest tests/ -v`
3. Coverage of all requirements:
```
INV-01, INV-02, INV-03, INV-04, INV-04a, INV-05, INV-06
BID-01, BID-02, BID-03, BID-04, BID-05, BID-06, BID-07, BID-08, BID-09, BID-10, BID-11, BID-12
```
</verification>

<success_criteria>
- [ ] tests/test_invest.py created with 10+ tests
- [ ] tests/test_bid.py created with 15+ tests
- [ ] All tests pass
- [ ] All 19 requirements have at least one test
- [ ] Full test suite still passes (no regressions)
- [ ] Tests cover edge cases (all pass, immediate resolution, etc.)
</success_criteria>

<output>
After completion, create `.planning/phases/03-invest-core-auction-flow/03-03-SUMMARY.md`
</output>
