**/__pycache__
**/.venv
**/*.pth
**/*.txt
!requirements.txt
**/*.so
**/*.o
**/*.c
**/*.h
**/*.cpp
**/*.html
!interp/examples/*.html
scratchpad/
checkpoints/
runs/
strategy_data/
interp/data/
backups/
gamelog.md
*.tar.gz
tests/games_18xx/data/*.json

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Codex creates a persistent file for some reason
.codex

# 18xx runtime dir: keep secrets local, but track templates.
runtime/*
!runtime/
!runtime/*.template.json
